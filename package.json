{
  "name": "story-mapping-generator-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "bun run --cwd packages/www check",
    "lint": "bun run lint:www",
    "lint:www": "bun run --cwd packages/www lint",
    "build": "bun run --cwd packages/www build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
